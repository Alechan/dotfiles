# Supuestamente, este archivo, .zprofile, sólos se ejecua una vez por sesión (a diferencia de .zshrc que se ejecutaría cada vez que abrís una nueva terminal)
#
# Swap caps lock con escape. Lo pongo acá porque lo voy a necesitar en todas las máquinas.
/usr/bin/setxkbmap -option "caps:swapescape"
